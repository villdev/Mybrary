<% if(locals.errorMessage != null) { %>
<!-- new -->
<%=locals.errorMessage%>
<!-- passed the value errorMessage from author controller but can directly access this key. Needed to use locals.key -->
<% } %>
